# 5- alphabet count
s = input("Enter string: ")
from collections import Counter
count = Counter(c.lower() for c in s if c.isalpha())
for char, cnt in sorted(count.items()):
    print(f"{cnt}{char.upper()}")
